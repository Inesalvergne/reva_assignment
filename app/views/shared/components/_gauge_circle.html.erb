<circle cx="18"
        cy="18"
        r="16"
        fill="none"
        class="<%= "stroke-current #{color}" %>"
        stroke-width="3"
        stroke-dasharray="<%= "#{dasharray}" %>"
        stroke-dashoffset="<%= "#{dashoffset}" %>"
        stroke-linecap="">
</circle>
